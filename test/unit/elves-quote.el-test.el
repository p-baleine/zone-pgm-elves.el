;;; elves-quote.el-test.el --- Tests for elves-quote.el

;;; Code:

(require 'elves-quote)

(ert-deftest elves-quote--test-elves-quote-offset-of ()
  (skip-unless nil))

(ert-deftest elves-quote--test-elves-quote-contents-of ()
  (skip-unless nil))

;;; elves-quote/elves-quote.el-test.el ends here
